{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://unosw.plus/schema/topics.schema.json",
  "title": "Topics",
  "description": "Themes used to classify content. The controlled vocabulary lives in conferences/unosw.json; this dataset adds descriptions and is the source for topic pages.",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["slug", "name"],
    "properties": {
      "slug": { "type": "string", "pattern": "^[a-z0-9-]+$" },
      "name": { "type": "string", "minLength": 1 },
      "description": { "type": "string" }
    }
  }
}
